(function() {
    var videoComponent = {
      template: `
      test
      `,
      controller: function(WizardService,$location) {
        var $ctrl = this;

      }

    };


    angular.module("app")
      .component("videoComponent", videoComponent)
  }());
